# 需求问答记录

> 自动追加。这一份不进上下文,只作留档。

- **Q**(q1) 装到哪个目录?
  - 选项:当前目录 /Users/hechenyu/explore/test-ide 下(clone 成 ./cppide) / 直接把当前目录变成仓库(clone 到 . ) / 别的路径(我来说)
  - **A**[answered] 当前目录 /Users/hechenyu/explore/test-ide 下(clone 成 ./cppide)
- **Q**(q2) "能用"到什么程度算完成?
  - 选项:能启动起来，我能实际打开界面/页面用 / 依赖装好、能构建成功就行，启动我自己来 / 装成全局命令，任意目录敲命令能用 / 其他(我来说)
  - **A**[answered] 能启动起来，我能实际打开界面/页面用
- **Q**(q3) 如果这项目缺系统级依赖(比如 cmake、Qt、Node、编译器工具链),允许直接装吗?
  - 选项:可以直接装（brew / npm -g / pip 等），不用问我 / 只能在项目目录内装，不动系统环境 / 要装系统级依赖时先停下来问我
  - **A**[answered] 只能在项目目录内装，不动系统环境
- **Q**(q4) 万一必须装系统级依赖才能跑起来,怎么办?
  - 选项:停下来，把缺的东西列清单给我，我自己装 / 想办法在项目目录内搞定（本地二进制 / venv / 便携版） / 到时候再说，先试了再问
  - **A**[answered] 想办法在项目目录内搞定（本地二进制 / venv / 便携版）
- **Q**(q5) 这个仓库是公开的吗?(我这边打不开页面,不确定是没网络权限还是仓库私有)
  - 选项:公开仓库，直接 clone 就行 / 私有，但本机已配 SSH key 或 gh 已登录 / 不确定
  - **A**[answered] 公开仓库，直接 clone 就行
- **Q**(q6) 如果 clone 下来发现项目本身有问题跑不起来(依赖版本冲突、代码报错),允许改仓库里的代码吗?
  - 选项:可以改，只要能跑起来 / 别改仓库代码，跑不起来就告诉我原因 / 构建/配置可以调，业务代码别动
  - **A**[answered] 构建/配置可以调，业务代码别动
- **Q**(q7) 目标被判为**无法达成**:工程实质是过关的,而且我自己复核了最要命的那条 —— 判产出物而不是源码:`file cppide` 是 `Mach-O 64-bit executable arm64`,`lsof -p 96040` 显示这个进程的 `txt` 就指向 `/Users/hechenyu/explore/test-ide/cppide/cppide`,起于 16:10,我在 16:15 看它还活着。一个 Linux ELF 不可能在 Darwin 上跑成 PID。这不是从 Makefile 里推断出来的,是产物在这台机器上真的在跑。源码也确实没动:`src/` 下所有 `.cpp`/`.h` 的 mtime 全是 15:32(clone 时刻),改的只有 Makefile(16:06),`.o`/`.d`(16:07)是被仓库跟踪的编译产物重建。

卡住的不是干活的质量,是**这一轮里原理上验不了的条目**。清单里本来就有三条标了 `[此环境无法验证]`(devtools console、人工点击、hechenyu 本人确认)。现在第四条也掉进了同一类:截图。`.flower/artifacts/` 下我 `find` 过,全盘零个 PNG;`screencapture` 因屏幕录制 TCC 未授权失败,我自己这边这条命令也执行不了。而授予 TCC 需要人去系统设置里点,那本身又是被边界禁止的系统改动 —— 换句话说,再派一轮子代理也变不出这张图。按规则这不许判通过,也不该判"未达成",它就是无法达成,该停下来交给人。

需要说清的是:AppleScript 读回的那段窗口文本我看了,是真实渲染的 TUI(行号栏、`src/main.cpp` 正文、`─[编译]─[运行]─[AI*]─[输入]─` 面板栏、`练习模式 │ main.cpp │ 1:1 │ C++` 状态栏),不是白屏也不是报错屏。干活的人没有拿它冒充"看图",这个自我克制是对的。但它替代不了清单要求的那条,我也不替他算过。
怎么办?
  - 选项:接受这个结果,就这样往下走 / 修改目标 / 你判断错了,继续做
  - **A**[answered] 接受这个结果,就这样往下走
