# Documentation site build dependencies.
# Pinned so a CI build never drifts from what was tested locally.
mkdocs-material>=9.5,<10
mkdocs-static-i18n>=1.3,<2
mkdocs-redirects>=1.2,<2
mkdocs-minify-plugin>=0.8,<1
